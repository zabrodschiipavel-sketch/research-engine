Тема: speculative decoding для ускорения инференса больших языковых моделей (LLM).

Контекст: рассматриваю добавление speculative decoding в инференс-пайплайн для локальной LLM (CPU/GPU, llama.cpp-совместимый стек) — хочу понять, насколько это применимо вне гигантских дата-центров и какие есть реалистичные варианты.

Разберись и напиши отчёт:
1. Как работает базовая идея speculative decoding (draft model + verification) и почему она вообще ускоряет инференс без потери качества.
2. 2-4 конкретных метода/статьи (например, оригинальный speculative decoding, Medusa, EAGLE, lookahead decoding или другие) — в чём их отличия и заявленные ускорения.
3. Практические ограничения: когда speculative decoding не даёт выигрыша (batch size, длина контекста, качество draft-модели), и что нужно для локального CPU-инференса конкретно.
4. Конкретные источники (статьи/репозитории) со ссылками/DOI или URL.

Формат отчёта: обычный текст с заголовками (##), без markdown-таблиц. Не более ~600 слов. В конце — список источников со ссылками.
